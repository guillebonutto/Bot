# EMA Pullback Bot Configuration
# Copy this to .env or use: python bots/bot_ema_pullback.py

# API Credentials (same for all bots)
POCKETOPTION_SSID=your_ssid_here
TELEGRAM_TOKEN=your_telegram_token
TELEGRAM_CHAT_ID=your_chat_id

# Bot-specific settings
BOT_NAME=ema_pullback
TIMEFRAMES=M1,M5
ML_THRESHOLD=0.65
SLEEP_INTERVAL=30

# Risk settings (from config.yaml)
# These are loaded from config.yaml, not .env
